extends ../layouts/main-layout.pug

block content
    main.centered
        h1 #{product.title}
        hr
        img(src=product.imageURL, alt=product.title)
        h2 #{product.price}
        p #{product.description}
        form(action="/cart", method="POSRT")
            button(type="submit").btn Add to Cart